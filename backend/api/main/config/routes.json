{
  "routes": [
    {
      "method": "GET",
      "path": "/main",
      "handler": "main.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/main",
      "handler": "main.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/main",
      "handler": "main.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
