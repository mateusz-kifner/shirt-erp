// Evaluate this
// Webhooks in penpot seems to not work 90% of time, webhook api is not usable in this state
// hook update-file is never triggered

// generic testing nextjs route
// export default async function Penpot(
//   req: NextApiRequest,
//   res: NextApiResponse,
// ) {
//   console.log(req.body);
//   res.status(200).json({ ok: true });
//   res.end();
// }

export type {};
